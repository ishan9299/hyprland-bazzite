name: Build Hyprland Artifacts

on:
  push:
    paths:
      - 'Hyprland.Containerfile'
  pull_request:
    paths:
      - 'Hyprland.Containerfile'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Install Podman
        run: |
          sudo apt-get update
          sudo apt-get install -y podman

      - name: Pull Hyprland Base Packages
        run: |
          podman pull \
            ghcr.io/${{ github.repository_owner }}/hyprland-base-packages:latest

      - name: Login to GHCR
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | \
            podman login ghcr.io \
            --username "${{ github.actor }}" \
            --password-stdin

      - name: Build Hyprland
        run: |
          podman build \
            --progress=plain \
            --file Hyprland.Containerfile \
            --tag ghcr.io/${{ github.repository_owner }}/hyprland-ecosystems:latest \
            --build-arg BUILDER_BASE_IMAGE=ghcr.io/${{ github.repository.owner }}hyprland-base-packages:latest \
            .

      - name: Push Hyprland Base Packages
        run: |
          podman push \
            ghcr.io/${{ github.repository_owner }}/hyprland-base-packages:latest
